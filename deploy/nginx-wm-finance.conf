server {
    listen 80;
    server_name wm-finance.pl www.wm-finance.pl;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name wm-finance.pl www.wm-finance.pl;

    ssl_certificate     /etc/letsencrypt/live/wm-finance.pl/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/wm-finance.pl/privkey.pem;

    # API -> Backend container (exposed on host via 127.0.0.1:8001)
    location /api/ {
        proxy_pass http://127.0.0.1:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;

        # For file uploads
        client_max_body_size 50M;
    }

    # Telegram webhook
    location /webhook/ {
        proxy_pass http://127.0.0.1:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Frontend (SPA) -> Frontend container on 127.0.0.1:3000
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
